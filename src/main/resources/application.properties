spring.application.name=chimhaha-clone
spring.config.import=file:./configs/config.properties

# Gateway 통합 — 외부 URL은 /api/post/* 로 prefix됨 (nginx가 라우팅).
# 내부 컨트롤러 @RequestMapping path는 무변경.
server.servlet.context-path=/api/post